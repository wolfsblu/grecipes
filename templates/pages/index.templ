package pages

import (
    "github.com/wolfsblu/grecipes/templates"
)

templ Index() {
    @templates.Layout() {
        <p>Hello from Index</p>
        <div hx-get="/recipes/" hx-trigger="load">
        </div>
    }
}